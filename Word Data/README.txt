COMMAND LINES OF INTEREST

Create virtual environment:
python -m venv .venv

Open virtual environment:
.venv\Scripts\activate.bat

Install necessary libraries (venv must be opened):
pip install -r requirements.txt

Run script:
python XXXX.py

Generate requirements text file (venv must be opened):
pip freeze > requirements.txt